import { TestHike } from "./hike"
import { Hike } from "codehike"

## Hellos

<Hike as={TestHike} >

```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(alignment: .leading) {
            Text("Turtle Rock")
                .font(.title)
            // !class(1:4) mark
            HStack {
                Text("Joshua Tree National Park")
                    .font(.subheadline)
            }
        }
    }
}


#Preview {
    ContentView()
}
```

</Hike>
